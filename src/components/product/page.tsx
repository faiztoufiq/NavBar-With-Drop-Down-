import { comonText } from "@/common/constant";

export default function Products() {
  return (
    <div className="h-screen flex justify-center items-center">
      <div>{comonText.productPage}</div>
    </div>
  );
}
